content type - paper book, fan translation, correction
source - from author, from translator, connection with author, connection with translator

- only one creation mode - single
	afterwards the entry can be moved to multiuser mode - separate button
- separate button for deletion
